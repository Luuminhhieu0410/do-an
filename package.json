{
  "name": "graduate",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx concurrently -c green,blue -n backend,frontend 'yarn workspace backend start' 'yarn workspace frontend dev' "
  },
  "author": "hieulm",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
